site_name: eWiz
site_url: !ENV READTHEDOCS_CANONICAL_URL

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [src]

extra_javascript:
  - javascript/readthedocs.js

nav:
  - Introduction: index.md
